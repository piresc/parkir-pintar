apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment
  namespace: pirescer-parkir-pintar
  labels:
    app: payment
    workload: service
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
        workload: service
    spec:
      containers:
        - name: payment
          image: ghcr.io/piresc/parkir-pintar/payment:latest-prod
          ports:
            - containerPort: 9093
              name: grpc
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          env:
            - name: GRPC_PORT
              value: "9093"
            - name: DB_SCHEMA
              value: "payment"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            tcpSocket:
              port: 9093
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 9093
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: pirescer-parkir-pintar
spec:
  selector:
    app: payment
  ports:
    - port: 9093
      targetPort: 9093
      name: grpc
